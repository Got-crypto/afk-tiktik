/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['lh3.googleusercontent.com', 'sportshub.cbsistatic.com', 'i.pinimg.com'],
  },
}

module.exports = nextConfig
